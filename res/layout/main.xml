<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bckgr"
    android:gravity="center_vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <ProgressBar
        android:id="@+id/progressCircle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/editTextURI"
        android:layout_below="@+id/editTextURI"
        android:layout_marginTop="22dp"
        android:animationResolution="0"
        android:visibility="invisible" />

    <ImageButton
        android:id="@+id/exitButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/progressDownload"
        android:layout_alignLeft="@+id/progressDownload"
        android:layout_marginBottom="18dp"
        android:background="@android:color/transparent"
        android:src="@drawable/exit"
        android:contentDescription="@null"
        android:onClick="onButtonClick" />

    <ImageButton
        android:id="@+id/infoButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/exitButton"
        android:layout_alignRight="@+id/progressDownload"
        android:background="@android:color/transparent"
        android:src="@drawable/logo_xread"
        android:contentDescription="@null"
        android:onClick="onButtonClick"
        android:visibility="invisible" />

	<ImageView
	    android:id="@+id/logoHeader"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:layout_alignParentTop="true"
	    android:layout_centerHorizontal="true"
	    android:layout_marginTop="30dp"
	    android:contentDescription="@null"
	    android:onClick="onButtonClick"
	    android:src="@drawable/logo_xread_title" />

	<AutoCompleteTextView
	    android:id="@+id/editTextURI"
	    android:layout_width="match_parent"
	    android:layout_height="wrap_content"
	    android:layout_below="@+id/logoHeader"
	    android:layout_centerHorizontal="true"
	    android:layout_marginTop="30dp"
	    android:background="#FFFFFF"
	    android:ems="10"
	    android:hint="@string/hint"
	    android:inputType="textUri|text|textAutoComplete"
	    android:textColor="#000000"
	    android:textColorHint="#707070"
	    android:textSize="20sp" >

	    <requestFocus />
	</AutoCompleteTextView>

	<ImageButton
	    android:id="@+id/startButton"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:layout_alignTop="@+id/cancelButton"
	    android:layout_centerHorizontal="true"
	    android:layout_marginTop="33dp"
	    android:background="@null"
	    android:contentDescription="@null"
	    android:onClick="onButtonClick"
	    android:src="@drawable/play" />

	<ImageButton
	    android:id="@+id/cancelButton"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:layout_alignLeft="@+id/editTextURI"
	    android:layout_alignTop="@+id/progressCircle"
	    android:background="@null"
	    android:contentDescription="@null"
	    android:onClick="onButtonClick"
	    android:src="@drawable/cancel" />

	<ProgressBar
	    android:id="@+id/progressDownload"
	    style="?android:attr/progressBarStyleHorizontal"
	    android:layout_width="match_parent"
	    android:layout_height="wrap_content"
	    android:layout_alignLeft="@+id/cancelButton"
	    android:layout_alignParentBottom="true" />

</RelativeLayout>